apiVersion: v1
kind: Service
metadata:
  name: guestbook
  namespace: guestbook
spec:
  type: NodePort
  selector:
    app: guestbook
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
